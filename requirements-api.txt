# Dipendenze del solo servizio API in sola lettura (deploy "vetrina" su Render).
# Per lo sviluppo in locale usa invece pyproject.toml:  uv sync
#
# Con il motore reale (COME_RENDERLO_REALE.txt) l'API non cambia. Solo se attivi i
# prezzi Massive aggiungi anche:  massive==2.8.0

fastapi==0.141.1
uvicorn[standard]==0.53.0
python-dotenv==1.2.3
pydantic==2.12.3
